pkg update -y
pkg install git unzip -y

rm -rf ~/fullthrottle64
mkdir -p ~/fullthrottle64
cd ~/fullthrottle64
unzip -o ~/storage/downloads/fullthrottle64-scummvm-r2.zip

git init
git config user.name "platynumx"
git config user.email "fullthrottle64@users.noreply.github.com"
git add .
git commit -m "FullThrottle64 real ScummVM R2"
git branch -M main
git remote add origin https://github.com/YOUR_GITHUB_USERNAME/fullthrottle64.git
git push -u origin main
